SCM1 --- A no-fluff tree-walking toy scheme

  $ cargo install --path .
  $ scm1 -h
  $ scm1 src/test.scm src/std.scm -i
    ...
  $ scm1 0.1.0
  $ || (: x (f (a b) (? (! a b) (m "hello world"))))
  $   x
  $ || (x #_ 1)
  $   hello world